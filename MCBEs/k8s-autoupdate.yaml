# ---------------------------------------------------------
# 1. 権限設定 (RBAC)
# ---------------------------------------------------------
apiVersion: v1
kind: ServiceAccount
metadata:
  name: minecraft-updater
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: minecraft-updater-role
  namespace: default
rules:
  # Deploymentを操作(再起動)する権限
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get", "list", "patch"]
  # Podの中でコマンド実行(RCON通知)するための権限
  - apiGroups: [""]
    resources: ["pods", "pods/exec"]
    verbs: ["get", "list", "create"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: minecraft-updater-binding
  namespace: default
subjects:
  - kind: ServiceAccount
    name: minecraft-updater
    namespace: default
roleRef:
  kind: Role
  name: minecraft-updater-role
  apiGroup: rbac.authorization.k8s.io

---
# ---------------------------------------------------------
# 2. スクリプト本体 (ConfigMap)
# ---------------------------------------------------------
apiVersion: v1
kind: ConfigMap
metadata:
  name: update-script
  namespace: default
data:
  check_and_update.sh: |
    #!/bin/bash
    set -e
    
    # ターゲット設定
    DEPLOYMENT="mc-bedrock"
    CONTAINER="minecraft" # Deployment内のコンテナ名
    
    # 最新バージョンの取得 (User-Agent偽装)
    # ※シンプル化のため、公式サイトからバージョン文字列を取得する簡易ロジック
    # 実際はitzgイメージの更新を待つのが安全だけど、ここでは「再起動してイメージプルさせる」戦略をとるね
    
    echo "🔍 Checking for updates..."
    
    # Pod名を取得
    POD_NAME=$(kubectl get pods -l app=minecraft-bedrock -o jsonpath="{.items[0].metadata.name}")
    
    if [ -z "$POD_NAME" ]; then
      echo "❌ Minecraft pod not found."
      exit 1
    fi

    # RCONで通知 (kubectl execを使用)
    echo "📢 Sending daily restart notification..."
    kubectl exec $POD_NAME -c $CONTAINER -- rcon-cli say "§e[System] §b定期メンテナンスの時間です。"
    kubectl exec $POD_NAME -c $CONTAINER -- rcon-cli say "§e[System] §fアップデート確認のため、1分後に再起動します。"
    
    sleep 60
    
    kubectl exec $POD_NAME -c $CONTAINER -- rcon-cli say "§e[System] §cRebooting now..."
    
    # Rollout Restart (これで新しいイメージがあれば取得される)
    echo "🔄 Triggering rollout restart..."
    kubectl rollout restart deployment/$DEPLOYMENT
    
    echo "✅ Restart command sent."

---
# ---------------------------------------------------------
# 3. CronJob (スケジュール実行)
# ---------------------------------------------------------
apiVersion: batch/v1
kind: CronJob
metadata:
  name: minecraft-auto-updater
  namespace: default
spec:
  # 毎日 朝4時 (JSTなら前日の19:00 UTCだけど、サーバーのタイムゾーンによる)
  # ここではサーバーがJST設定と仮定して "0 4 * * *"
  schedule: "0 4 * * *"
  jobTemplate:
    spec:
      template:
        spec:
          serviceAccountName: minecraft-updater # 権限を持ったユーザーを使う
          containers:
            - name: updater
              # kubectl が入っている軽量イメージを使う
              image: bitnami/kubectl:latest
              command: ["/bin/bash", "/scripts/check_and_update.sh"]
              volumeMounts:
                - name: script-volume
                  mountPath: /scripts
          restartPolicy: OnFailure
          volumes:
            - name: script-volume
              configMap:
                name: update-script
                defaultMode: 0755 # 実行権限をつける